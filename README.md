primeiro dê : npm install
depois: npm install -g eas-cli
e depois dê  npx expo install expo-sensors   para instalar dos sensores
eas login para fazer login e por ultimo eas build -p android --profile preview   para buildar o app